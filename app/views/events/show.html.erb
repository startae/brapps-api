
	<%= image_tag @event.large_image %>
	<%= @event.title %>
	<hr />
	<% @event.speakers.each do |speaker|%>
		<%= speaker.name %>,
		<%= speaker.company %>
		<br/>
	<%end%>
	<% if @event.first_day?%>
		Sexta <%= @event.time %>
	<% else %>
		Sábado <%= @event.time %>
	<% end %>


